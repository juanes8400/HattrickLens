__pycache__/
.venv/
node_modules/
.next/
.env
.env.*
!.env.example
*.pyc
*.log
*.db
# Copias de la base con datos reales del club: mismo criterio que *.db, que
# no las pillaba por el sufijo de fecha (dev.db.bak-20260815).
*.db.bak*
*.db-*
*.sqlite
*.sqlite3
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.import_linter_cache/
*.tsbuildinfo
dist/
coverage/
.claude/
.idea/
.vscode/
.DS_Store
Thumbs.db

# Capturas de pantalla del propio uso: 2,6 MB que engordan cada clonación, se
# quedan obsoletas a la primera pantalla que cambia y no hacen falta para
# ejecutar nada. Los fixtures XML de tests SÍ se versionan: sin ellos la suite
# no corre en otra máquina.
artifacts/

# Claves generadas para el despliegue. Nunca al repositorio.
claves-render.txt
